# Reverse Shell Generator
Generate reverse shell in local in: bash, python, php/web, perl, etc... (You can add more payloads)
> use: "python3 revgen.py --help" and install all libraries with: "pip install -r requirements.txt"
